# Environment Configuration for Observability Platform
# Copy this file to .env and adjust values as needed

# === Kafka Configuration ===
# For containers: kafka:9092, For local dev: localhost:29092
KAFKA_BROKER=kafka:9092
KAFKA_RAW_EVENTS_TOPIC=raw_events
KAFKA_AGGREGATED_METRICS_TOPIC=aggregated_metrics
KAFKA_ANOMALIES_TOPIC=anomalies

# === DuckDB Configuration ===
# For containers: /data/observability.db, For local dev: ./data/observability.db
# Note: DuckDB is single-writer. All services include retry logic for lock conflicts.
DUCKDB_PATH=/data/observability.db

# === LLM Configuration ===
# For containers: http://ollama:11434, For local dev: http://localhost:11434
# Note: If behind VPN/proxy, you may need to disconnect or set NO_PROXY
OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=phi

# === Spark Configuration ===
SPARK_MASTER=local[*]

# === Airflow Configuration ===
AIRFLOW_HOME=/opt/airflow
AIRFLOW_UID=50000

# === Proxy Settings ===
# Uncomment and configure if behind corporate proxy
# HTTP_PROXY=http://your-proxy.company.com:8080
# HTTPS_PROXY=http://your-proxy.company.com:8080
# http_proxy=http://your-proxy.company.com:8080
# https_proxy=http://your-proxy.company.com:8080

# For local Ollama access, add localhost to NO_PROXY
# NO_PROXY=localhost,127.0.0.1

